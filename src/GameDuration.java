public class GameDuration {
    
    /*
    computeifabsent:
    if the key is used and have a value, will return the value
    if the key is not used, it will return to the value that you set for it

    List<Boardgame> games = gameByDuration.computeIfAbsent(
        key, k -> new linekedList<>()
    )
    
     */

     
      /* things that can convert into stream

      map - either stream the key or the value
        map.keyset().Stream()       - this is to retrieve the key
            .map(k-> map.get(k))    - this is to retrieve the value

        
        map.values().stream()  - to retireve value


        for(String K: map.keyset()){
            string v = map.get(k)
        }
      list
      sets */
      
}
